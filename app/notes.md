- Does  if params[:username] == "" || params[:password] == ""
equal:  if params[:username] == nil || params[:password] == nil, if not why not?

- why use 'redirect to' and redirect '/', what is the diff?

- 
